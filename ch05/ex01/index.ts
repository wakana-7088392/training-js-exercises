{
  let callSameNumber = () => {
    const sameNumber = 1;
    let array: number[] = [];
    for (let i = 0; i < 10; i++) {
      const sameNumber = 1;
      let resultArray = array.push(sameNumber);
    }
  };
}
