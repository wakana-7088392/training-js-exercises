## 12.1 以下の関数 counterIter() 及び counterGen() を利用して、イテレータ及びジェネレータに対して、どのような操作をした時にどの部分が実行されるのか、動作を確認しなさい。

# 各検証の結果

- 明示的にイテレータインタフェース のメソッドを呼んだり、間接的に呼んだりする
  - 明示的に呼び出すことはできた。
  - 間接的に呼び出す、というもののイメージがつかずうまくできなかった。ジェネレータだと、引数より多くの回数呼ばれるとfinally文が動いており、間接的にreturn文が呼ばれていたかもしれない。
- ジェネレータ関数によって生成されたオブジェクトがイテレータインタフェースを満たしていることを確認する
  - 確認できた。
  - 必須であるnext()以外にも、任意であるreturn()やthrow()も使用できた。
- return() や throw() がどのようなときに呼ばれるのか確認する
  - ジェネレータで直接return()を呼び出すと、try~catch文のfinally文が呼び出された。
  - ジェネレータで直接throw()を呼び出すと、try~catch文のcatch文が呼び出された
- ジェネレータ関数の中身がどのタイミングで初めて実行されるか確認する
  - 変数に引数を指定した関数を代入する際に「counterGen」がログ出力されていたので、変数宣言時？

# メモ

- イテレーションインターフェースの仕様翻訳
  - インターフェースとは、特定の仕様に一致するプロパティキーのセットです。インターフェースの仕様に記載されているすべてのプロパティを提供するオブジェクトは、そのインターフェースに準拠しています。インターフェースは、独立したオブジェクトとして表現されるものではありません。任意のインターフェースに準拠する個別に実装されたオブジェクトが多数存在する可能性があります。個々のオブジェクトは、複数のインターフェースに準拠する場合があります。
- 必須プロパティ
  - next
- 任意プロパティ
  - return
  - throw
